{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "ANTHROPIC_API_KEY",
      "description": "Anthropic Claude API key from console.anthropic.com — powers the agentic chat/proposal features. A dev placeholder is generated so the app boots; replace it with the real key for AI to work.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
